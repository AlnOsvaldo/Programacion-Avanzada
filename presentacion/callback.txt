BOOL CALLBACK procedure(HWND hDlg, UINT msg, WPARAM wParam, LPARAM lParam)
{
	// static HWND hUsua = 0;

	switch (msg)
	{
	case WM_INITDIALOG:
	{
		// hUsua = GetDlgItem(hDlg, IDC_USUA);
		return true;
	}
	case WM_COMMAND:
		switch (LOWORD(wParam))
		{
		case IDC_LOGIN:
			return true;

		case IDC_EXIT:

			return true;
		}
		return true;
		// If the dialog was closed (user pressed 'X' button)
		// then terminate the dialog.
	case WM_CLOSE:
		return true;
	case WM_DESTROY:
		return true;
	}
	return false;
}